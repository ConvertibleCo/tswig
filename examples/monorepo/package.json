{
  "name": "my-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build-package --prefix packages/package1 && npm run build-package --prefix packages/package2",
    "build-package": "npm run build --if-present"
  }
}
